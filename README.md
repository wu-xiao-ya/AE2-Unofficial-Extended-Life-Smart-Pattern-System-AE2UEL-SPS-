写在最前面，该mod主要使用Copilot开发
Written at the front, the mod is mainly developed using Copilot

# AE2UEL 智能样板系统 (Smart Pattern System)
*AE2 Unofficial Extended Life 智能样板系统 - 基于矿物辞典的通配符合成样板*

> 🎉 **v1.0.0 首个正式版发布！** 详见 [发布指南](RELEASE_GUIDE.md) 和 [更新日志](CHANGELOG.md)

[![Minecraft](https://img.shields.io/badge/Minecraft-1.12.2-green.svg)](https://www.minecraft.net/)
[![Forge](https://img.shields.io/badge/Forge-14.23.5.2847-red.svg)](https://files.minecraftforge.net/)
[![Java](https://img.shields.io/badge/Java-8+-blue.svg)](https://adoptium.net/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Version](https://img.shields.io/badge/Version-1.0.0-brightgreen.svg)](CHANGELOG.md)

一个为 AE2UEL (Applied Energistics 2 Unofficial Extended Life) 设计的智能样板系统，通过矿物辞典实现通配符匹配，让单个样板可以自动匹配整个矿物辞典系列（如所有金属锭→板的转换）。

## ✨ 核心特性

### 🎯 智能样板扩展
- **矿物辞典通配符**: 利用 Forge 矿物辞典系统，支持 `ingot*`、`plate*` 等通配符模式
- **自动批量生成**: 一个样板可自动扩展为多个具体配方（如 `ingot*→plate*` 根据矿物辞典自动识别所有已注册的金属配方）
- **AE2UEL 完全兼容**: 实现 `ICraftingPatternItem` 接口，无缝集成 AE2 ME 接口
- **样板扩展器方块**: 专用方块用于编辑和管理智能样板

### 🖥️ 直观的编辑界面
- **方块右键GUI**: 手持样板右键点击样板扩展器方块打开编辑界面
- **拖拽式操作**: 从物品栏拖拽物品到输入/输出槽进行样板定义
- **数量设置**: 支持1-64的输入/输出数量调节（+/- 按钮）
- **实时显示**: 即时显示矿物辞典类型和当前数量设置
- **NBT持久化**: 样板数据自动保存并显示在物品提示中

### 🔧 技术亮点
- **客户端-服务器同步**: 完整的网络包系统确保数据一致性
- **性能优化**: 启动时预加载矿物辞典缓存，运行时零开销
- **错误容错**: 完善的空指针检查和异常处理机制

## 📦 安装要求

### 必需依赖
- **Minecraft**: 1.12.2
- **Minecraft Forge**: 14.23.5.2847+
- **AE2UEL**: v0.56.8-novaeng_ver 或更高版本

### 推荐配置
- **Java**: 8+ (推荐使用 Temurin/Adoptium OpenJDK)
- **内存**: 至少 4GB 分配给 Minecraft
- **其他模组**: 支持矿物辞典的金属模组（热力膨胀、通用机械等）

## 🚀 快速开始

### 安装步骤
1. 安装 Minecraft 1.12.2 和 Forge 14.23.5.2847+
2. 下载并安装 AE2UEL v0.56.8+
3. 将本模组 jar 文件放入 `mods` 文件夹
4. 启动游戏

### 基本使用流程

#### 1. 获取物品
```
/give @s sampleintegration:pattern_integrations    # 智能样板物品
/give @s sampleintegration:pattern_expander        # 样板扩展器方块
```

#### 2. 编辑样板
1. **放置样板扩展器方块**在世界中
2. **手持智能样板物品**，右键点击方块打开编辑界面
3. **拖拽输入物品**到左侧输入槽（例如：铁锭）
4. **拖拽输出物品**到右侧输出槽（例如：铁板）
5. **调整数量**: 使用输入/输出下方的 +/- 按钮设置数量（1-64）
6. 系统自动识别矿物辞典并生成通配符样板：`ingot* x1 → plate* x1`

#### 3. 使用样板
1. 将编码好的智能样板插入 **AE2 ME 接口**
2. ME 系统会自动识别并扩展样板为 19 个具体配方
3. 现在可以使用 ME 系统自动合成所有支持的金属板

#### 4. 清除样板
- **Shift + 右键**点击手中的样板物品即可清除 NBT 数据

## 📖 详细功能说明

### 支持的通配符类型
当前版本支持以下矿物辞典前缀的通配符：
- `ingot*` → 所有金属锭
- `plate*` → 所有金属板
- `nugget*` → 所有金属粒
- `block*` → 所有金属块
- `gear*` → 所有齿轮
- `rod*` → 所有杆/棒
- `wire*` → 所有线缆

### 自定义配方映射 (config)
当 CRT 或其他脚本写法不规范时，可在配置中显式指定配方对，系统会优先使用这些配置：

1. 启动一次游戏，生成配置文件 `config/sampleintegration.cfg`
2. 打开配置文件，在 `pattern_expander` 类别下添加：

```ini
customRecipePairs = [
  "inogtCopper -> plateCopper",
  "ingotTin -> plateTin",
  "ingotBronze -> gearBronze"
]
```

说明：
- 左右两侧是明确的矿辞条目，不需要通配符
- 配置会先被读取，再进行矿辞扫描补全
- 支持不规范前缀（如 `inogt`）

### 样板扩展示例
**输入**: `ingotCopper` (铜锭) → **输出**: `plateCopper` (铜板)

系统自动扩展为 19 个配方：
```
铁锭 → 铁板
铜锭 → 铜板
金锭 → 金板
锡锭 → 锡板
银锭 → 银板
铅锭 → 铅板
... (根据矿辞自动扩展)
```

> **提示**: 只要矿物辞典中存在 `ingotX` ↔ `plateX` 等对应条目，系统会自动识别并扩展，不限制具体材料种类。支持所有使用矿物辞典的模组（热力膨胀、通用机械、沉浸工程等）。实际支持的种类由游戏中已安装的模组决定。

### GUI 界面详解

**布局说明**:
```
┌─────────────────────────────────────┐
│  智能样板编辑器                      │
├─────────────────────────────────────┤
│ 输入:                    输出:       │
│ ┌─────┐               ┌─────┐      │
│ │     │               │     │      │  ← 输入/输出物品槽
│ └─────┘               └─────┘      │
│                                     │
│ 输入: 物品名称 (矿物类型) x数量      │  ← 实时信息显示
│ 输出: 物品名称 (矿物类型) x数量      │
│                                     │
│ [-]  input  [+]    [-] output [+]   │  ← 数量调节按钮
├─────────────────────────────────────┤
│        玩家背包和快捷栏               │
└─────────────────────────────────────┘
```

**操作方式**:
- **拖拽物品**: 从下方背包拖拽到上方输入/输出槽
- **调整数量**: 点击 `-` 减少，`+` 增加（范围：1-64）
- **关闭GUI**: 按 ESC 键，物品自动返回背包
- **Shift+右键样板物品**: 清除所有NBT数据

### 物品信息显示
编码后的样板会在鼠标悬停时显示：
```
智能样板
§a已标记: 铜锭 → 铜板
§7输入: ingotCopper §7x§f1
§7输出: plateCopper §7x§f1
§7右键打开编辑器
```

## 🛠️ 开发环境

基于 **[RetroFuturaGradle](https://github.com/GTNewHorizons/RetroFuturaGradle)** 构建系统：

- **Java**: 8+ (编译目标)
- **Gradle**: 9.2.1+
- **RetroFuturaGradle**: 2.0.2+
- **Mixin支持**: 完整 Mixin 和 Coremod 支持

### 开发设置

```bash
# 克隆项目
git clone <repository-url>
cd ForgeDevEnv

# 构建项目
./gradlew build

# 启动客户端测试
./gradlew runClient

# 启动服务器测试
./gradlew runServer
```

### 可用任务
```bash
./gradlew build          # 构建项目
./gradlew runClient      # 启动客户端
./gradlew runServer      # 启动服务器
./gradlew compileJava    # 编译Java源码
./gradlew jar            # 打包JAR文件
```

## 📁 项目结构

```
src/main/java/com/lwx1145/techstart/
├── TechStart.java                      # 主模组类，生命周期管理
├── ItemTest.java                       # 智能样板物品定义
├── BlockPatternExpander.java           # 样板扩展器方块
├── TileEntityPatternExpander.java      # 样板扩展器TileEntity
├── BlockPatternTerminal.java           # 模式终端方块（预留）
├── ContainerPatternEditor.java         # GUI容器逻辑
├── GuiPatternEditor.java               # GUI界面渲染
├── GuiHandler.java                     # GUI注册处理
├── SmartPatternDetails.java            # ICraftingPatternDetails实现
├── VirtualPatternExpander.java         # 虚拟样板扩展器（AE2集成）
├── OreDictRecipeCache.java             # 矿物辞典缓存系统
├── PacketHandler.java                  # 网络包管理器
├── PacketUpdatePatternCount.java       # 数量同步网络包
└── ModConfig.java                      # 配置文件处理

src/main/resources/
├── assets/sampleintegration/
│   ├── lang/
│   │   ├── en_us.lang                  # 英文语言文件
│   │   └── zh_cn.lang                  # 中文语言文件
│   ├── textures/
│   │   ├── items/                      # 物品材质
│   │   ├── blocks/                     # 方块材质
│   │   └── gui/                        # GUI材质
│   ├── models/
│   │   ├── item/                       # 物品模型
│   │   └── block/                      # 方块模型
│   └── blockstates/                    # 方块状态定义
├── mcmod.info                          # 模组元数据
└── pack.mcmeta                         # 资源包元数据

gradle/
├── scripts/
│   ├── dependencies.gradle             # 依赖配置
│   ├── extra.gradle                    # 额外配置
│   └── helpers.gradle                  # 辅助脚本
└── wrapper/
    └── gradle-wrapper.properties       # Gradle包装器配置
```

## 🔧 配置说明

### 模组信息
主要配置在 `gradle.properties`：
```properties
mod_version=1.0.0
mod_id=sampleintegration
mod_name=AE2UEL Smart Pattern System
mod_group=com.lwx1145.techstart
```

### 依赖管理
依赖配置位于 `gradle/scripts/dependencies.gradle`：
- **AE2UEL**: 运行时依赖（deobfCompile）
- **Mixin**: Mixin支持和加载器
- **JEI**: 可选兼容性（运行时）

### 构建输出
编译后的文件位于：
```
build/libs/
└── sampleintegration-1.0.0.jar         # 最终模组文件
```

## 🐛 故障排除

### 常见问题

**Q: 模组无法加载？**
A: 
- 确保已安装 AE2UEL v0.56.8+ 依赖
- 检查 Forge 版本是否为 14.23.5.2847+
- 查看日志文件 `logs/latest.log` 获取详细错误信息

**Q: GUI 界面无法打开？**
A: 
- 必须手持智能样板物品并右键点击样板扩展器方块
- 确保方块已正确放置且未被破坏
- 尝试重新获取一个新的样板物品

**Q: 拖拽物品没有反应？**
A: 
- 确保使用的是从方块打开的GUI（不是物品右键）
- 尝试先将物品放入槽位再取出，确认槽位可以交互
- 检查物品是否有矿物辞典条目（使用 `/ct hand` 查看）

**Q: 数量设置不保存？**
A: 
- 已在 v1.0.0 修复，确保使用最新版本
- 数量修改通过网络包同步，单人游戏无此问题
- 多人游戏确保服务端也安装了模组

**Q: 样板无法插入 ME 接口？**
A: 
- 确保样板已正确编码（物品提示显示"已标记"）
- 检查 AE2UEL 版本兼容性（推荐 v0.56.8+）
- 确认 ME 接口有空闲的样板槽位

**Q: 样板不会自动扩展为多个配方？**
A: 
- 确保使用的是通配符样板（如 `ingot*→plate*`）
- 检查矿物辞典中是否存在对应条目
- 查看 ME 终端确认配方是否已生成

### 调试技巧

**启用详细日志**：
在游戏目录 `config/sampleintegration.cfg` 中设置：
```
debug_mode=true
```

**查看矿物辞典条目**：
```
/ct hand                 # 查看手中物品的矿物辞典条目
/ct oredict <name>       # 查看特定矿物辞典的所有物品
```

**重新生成配置文件**：
删除 `config/sampleintegration.cfg`，重启游戏自动生成默认配置。

## 🎨 材质准备清单

### 发布前需要准备的材质文件

**物品材质**:
- `assets/sampleintegration/textures/items/item_test.png` (16x16)
  - 智能样板物品图标
  - 推荐风格：类似AE2样板但带有"智能"标识

**方块材质**:
- `assets/sampleintegration/textures/blocks/block_pattern_expander.png` (16x16)
  - 样板扩展器方块纹理（6个面可用同一张或分别指定）
  - 推荐风格：机械风格，与AE2方块协调

- `assets/sampleintegration/textures/blocks/block_pattern_terminal.png` (16x16)
  - 模式终端方块纹理（预留功能）

**GUI材质**:
- `assets/sampleintegration/textures/gui/pattern_editor.png` (256x256)
  - GUI背景纹理
  - 当前尺寸：176x220像素
  - 需包含：槽位边框、标题栏、背景底纹

### 材质建议
- **分辨率**: 标准16x16（或32x32高清）
- **风格**: 与AE2原版材质保持一致
- **颜色**: 使用AE2的蓝绿色调
- **格式**: PNG，支持透明通道

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

### 开发规范
- 遵循 Minecraft Forge 开发最佳实践
- 保持代码注释完整（中文或英文）
- 提交前进行充分测试

### 测试清单
- [ ] 客户端启动无崩溃
- [ ] GUI 正常打开和关闭
- [ ] 物品拖拽功能正常
- [ ] 数量设置保存正确
- [ ] ME 接口识别样板
- [ ] 样板扩展为多个配方
- [ ] 配置文件正常加载

### 代码贡献流程
1. Fork 本项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 [MIT License](LICENSE) 许可证。

## 🙏 致谢

- **AE2UEL 团队**: 提供优秀的 AE2 扩展版本
- **Minecraft Forge 社区**: 强大的模组开发框架
- **CleanroomMC**: RetroFuturaGradle 构建系统
- **矿物辞典系统**: Forge 统一物品标签系统

## 📞 联系方式

- **问题反馈**: 在 GitHub 提交 Issue
- **功能建议**: 在 GitHub 提交 Feature Request
- **交流讨论**: 加入 Minecraft 模组开发社区

---

**版本**: v1.0.0 | **Minecraft**: 1.12.2 | **Forge**: 14.23.5.2847+ | **AE2UEL**: v0.56.8+

*基于 CleanroomMC 的 RetroFuturaGradle 构建* | *完全开源，MIT 许可*

